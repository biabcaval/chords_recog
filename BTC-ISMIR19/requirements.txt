# Core ML
torch >= 2.0.0
numpy >= 1.24.0

# Audio processing
librosa >= 0.10.0
soundfile >= 0.12.0
pyrubberband >= 0.3.0

# Data & config
pandas >= 2.0.0
pyyaml >= 6.0

# Evaluation
mir_eval >= 0.7

# Utilities
tqdm >= 4.65.0
scipy >= 1.10.0

# Logging & monitoring
wandb >= 0.15.0

# Optional (MIDI support)
pretty_midi >= 0.2.10
